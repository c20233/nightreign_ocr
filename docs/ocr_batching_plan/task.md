# タスクリスト: OCRのバッチ処理化（縦結合方式）と解像度非依存化

- [x] `main.py` にフルHD(1920x1080)を基準とした領域・枠の「割合（比率）」の定数を定義する
- [x] 動画のフレーム解像度を取得し、動的に `roi_y1, roi_y2, roi_x1, roi_x2` を計算する処理を実装する
- [x] メインループ外にバッファリスト `batch_frames` を準備する
- [x] テキスト変化を検知した際、OCRを即実行せずに画像を `batch_frames` に追加する
- [x] `batch_frames` が4件に達したか、動画が終了した場合に一括処理を実行する
- [x] バッファ内の画像を `np.vstack` で縦に結合する
- [x] 結合画像に対して1回だけ `ocr.predict` を実行する
- [x] OCRのY座標結果と動的計算されたROI高さから、所属する元のフレームインデックスを逆算する
- [x] 計算された相対Y座標と割合を使って元の効果領域（タイトル〜効果3）へ振り分ける
- [x] 色判定やJSONの出力形式が維持されているか確認する
