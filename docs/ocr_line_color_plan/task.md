# タスクリスト: 行単位の文字色判定とメリット/デメリット分離

- [x] `main.py` のOCR結果パース部分を修正し、行ごとのBounding Box座標を取得する
- [x] Bounding Box座標を使って元の `roi_frame` から1行単位の画像を切り抜く
- [x] 切り抜いた1行単位の画像に対して `classify_text_color` を実行する
- [x] 同じ枠内の行を評価し、色が同じ場合はテキストを結合、色が違う場合は分離する
- [x] 出力されるJSONフォーマットをリスト形式に変更する
